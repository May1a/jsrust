{
    "type": "module",
    "scripts": {
        "test": "bun test",
        "compile": "bun build main.js --minify --target=bun --outdir dist",
        "typecheck": "bunx oxlint --type-aware --type-check",
        "format": "bunx prettier -w ."
    },
    "devDependencies": {
        "@types/bun": "^1.3.9",
        "@types/node": "^25.3.0",
        "globals": "^17.3.0",
        "oxlint": "^1.50.0",
        "oxlint-tsgolint": "^0.15.0",
        "typescript": "^6.0.0-beta"
    },
    "prettier": {
        "tabWidth": 4
    }
}
