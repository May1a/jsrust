module main {

  struct Vec { (), (), () }

  fn f0 test_example() -> () {
    b0 (entry):
      v1: bool = bconst true
      br_if v1, then: b1, else: b2
  
    b1 (if_then):
      v2: i32 = iconst 1
      br b3
  
    b2 (if_else):
      v3: i32 = iconst 2
      br b3
  
    b3 (if_merge):
      v4: i8            = iconst 0
      v5: ptr<struct _> = alloca struct _
      store v4 -> v5
      ret
  }

}
