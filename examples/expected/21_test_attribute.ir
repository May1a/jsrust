module main {

  strings {
    s0 = "a = {}"
    s1 = "x = {}"
  }

  fn f178369 test_basic() -> () {
    b0 (entry):
      v0: i32      = iconst 1
      v1: i32      = iconst 2
      v2: i32      = iadd v0, v1
      v3: ptr<i32> = alloca i32
      store v2 -> v3
      v5: ptr      = sconst s0
      v6: i32      = load v3
      v7: i64      = iconst 1
      call f862609(v5, v7, v6)
      ret
  }

  fn f799213 test_multiplication() -> () {
    b0 (entry):
      v9: i32       = iconst 4
      v10: i32      = iconst 5
      v11: i32      = imul v9, v10
      v12: ptr<i32> = alloca i32
      store v11 -> v12
      v14: ptr      = sconst s1
      v15: i32      = load v12
      v16: i64      = iconst 1
      call f862609(v14, v16, v15)
      ret
  }

}
