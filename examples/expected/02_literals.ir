module main {

  strings {
    s0 = "hello"
  }

  enum Option { [], [()] }

  fn f949693 test_example() -> () {
    b0 (entry):
      v0: i32       = iconst 42
      v1: ptr<i32>  = alloca i32
      store v0 -> v1
      v3: f64       = fconst 3.14
      v4: ptr<f64>  = alloca f64
      store v3 -> v4
      v6: bool      = bconst true
      v7: ptr<bool> = alloca bool
      store v6 -> v7
      v9: ptr       = sconst s0
      v10: ptr<ptr> = alloca ptr
      store v9 -> v10
      ret
  }

}
