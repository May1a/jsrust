module main {

  struct Vec { (), (), () }

  fn f0 test_example() -> () {
    b0 (entry):
      v1: i32           = iconst 10
      v2: ptr<struct _> = alloca struct _
      store v1 -> v2
      v4: struct _      = load v2
      v5: ptr<struct _> = alloca struct _
      store v2 -> v5
      v7: struct _      = load v5
      v8: i64           = load v7
      v9: ptr<struct _> = alloca struct _
      store v8 -> v9
      ret
  }

}
